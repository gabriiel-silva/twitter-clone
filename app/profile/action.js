
/* export default function Update() {

    const { dados } = await supabase
        .from('profiles')
        .update({ full_name: { 'inputValue'} })
        .eq('some_column', 'someValue')
        .select()
    return;
}
*/